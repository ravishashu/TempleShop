@model ProductListViewModel

<h1>@Model.CurrentCategory</h1>

<div class="row row-cols-1 row-cols-md-3 g-4">

    @foreach (var shopItem in Model.Items)
    {
       <partial name="_ShopItem" model="shopItem" />
    }

</div>